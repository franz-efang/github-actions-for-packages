# This file wont do us much good in this location, make sure you change the path as directed to continue!

name: Docker CD

on:
  push:
jobs:
  curl:
    runs-on: ubuntu-latest
    steps:
    - name: curl
      uses: wei/curl@master
      with:
        args: -X POST https://hub.docker.com/api/build/v1/source/8acea5d0-061f-4343-b521-cc7094a10a9e/trigger/7485a56c-2f6f-4330-8da8-c275667d0244/call/
